[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install --production --ignore-scripts"]

[phases.build]
cmds = ["echo 'Backend deployment - no build needed'"]

[start]
cmd = "node server.js"

