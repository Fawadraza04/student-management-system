[providers]
node = "20"

[phases.install]
cmds = [
  "npm install --production --ignore-scripts"
]

[phases.build]
cmds = ["echo 'Backend deployment - skipping frontend build'"]

[start]
cmd = "node server.js"
