[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "npm install express mongoose cors body-parser bcryptjs jsonwebtoken --production"
]

[phases.build]
cmds = ["echo 'Backend deployment - no build needed'"]

[start]
cmd = "node server.js"
